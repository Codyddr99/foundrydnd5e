<section>
    {{~#if hasConcentration}}
    <fieldset>
        <legend>{{ localize "DND5R.Concentration" }}</legend>
        {{#each fields}}
            {{ formField field name=name value=value options=options input=input }}
        {{/each}}
    </fieldset>
    {{> "dnd5r.activity-usage-notes" }}
    {{/if~}}
</section>
