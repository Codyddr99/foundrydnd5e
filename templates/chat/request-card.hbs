<div class="dnd5r2 chat-card request-card">
    <div class="card-buttons">
        <button {{dnd5r-dataset dataset}}>
            <span class="visible-dc">{{{buttonLabel}}}</span>
            <span class="hidden-dc">{{{hiddenLabel}}}</span>
        </button>
    </div>
</div>
